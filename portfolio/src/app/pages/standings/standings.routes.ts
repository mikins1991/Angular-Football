import { Routes } from '@angular/router';
import { StandingsContainer } from './standings.container';

export const StandingsRoutes: Routes = [
  { path: 'standings', component: StandingsContainer },
];
