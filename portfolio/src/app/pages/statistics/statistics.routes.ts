import { Routes } from '@angular/router';
import { StatisticsContainer } from './statistics.container';

export const StatisticsRoutes: Routes = [ { path: 'statistics', component: StatisticsContainer } ];
